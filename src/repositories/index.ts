import { CityRepository } from './city'
import { CustomerRepository } from './customer'

const cityRepository = new CityRepository()
const customerRepository = new CustomerRepository()

export { cityRepository, customerRepository }
